services:
  docmost-mcp:
    build: .
    ports:
      - "3000:3000"
    environment:
      DOCMOST_BASE_URL: ${DOCMOST_BASE_URL}
      DOCMOST_API_TOKEN: ${DOCMOST_API_TOKEN}
      PORT: 3000
    restart: unless-stopped
